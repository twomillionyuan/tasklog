{
  "name": "spotlog",
  "private": true,
  "workspaces": [
    "api"
  ],
  "scripts": {
    "build": "npm --prefix api install --include=dev && npm --prefix api run build",
    "start": "node api/dist/server.js",
    "load:test": "node scripts/load-test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
